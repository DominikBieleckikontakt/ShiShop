import { CustomCarousel, ShopPanel } from "@/components/server";
import React from "react";

const page = () => {
  return (
    <main>
      <CustomCarousel />
      <ShopPanel />
    </main>
  );
};

export default page;
